# Firebase Functions Environment Configuration
# Store sensitive configuration here and deploy with: firebase functions:config:set

# Email configuration for password reset
# To set these values, run:
# firebase functions:config:set gmail.user="gaussanalyticske@gmail.com"
# firebase functions:config:set gmail.password="YOUR_GMAIL_APP_PASSWORD"

# Note: Use Gmail App Password, not your regular Gmail password
# Generate an App Password at: https://support.google.com/accounts/answer/185833

# Example deployment commands:
# firebase functions:config:set gmail.user="gaussanalyticske@gmail.com"
# firebase functions:config:set gmail.password="your-app-password-here"
# firebase deploy --only functions

# Other environment variables
# firebase functions:config:set app.name="Fedha Budget Tracker"
# firebase functions:config:set app.support_email="gaussanalyticske@gmail.com"

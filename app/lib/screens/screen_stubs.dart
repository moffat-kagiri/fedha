import 'package:flutter/material.dart';

class AuthWrapper extends StatelessWidget {
  const AuthWrapper({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Auth Wrapper - Under Development'),
      ),
    );
  }
}

class OnboardingScreen extends StatelessWidget {
  const OnboardingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Onboarding Screen - Under Development'),
      ),
    );
  }
}

class SignInScreen extends StatelessWidget {
  const SignInScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Sign In Screen - Under Development'),
      ),
    );
  }
}

class PermissionSetupScreen extends StatelessWidget {
  const PermissionSetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Permission Setup Screen - Under Development'),
      ),
    );
  }
}

class TextRecognitionSetupScreen extends StatelessWidget {
  const TextRecognitionSetupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Text Recognition Setup Screen - Under Development'),
      ),
    );
  }
}

class TransactionCandidatesScreen extends StatelessWidget {
  const TransactionCandidatesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Transaction Candidates Screen - Under Development'),
      ),
    );
  }
}

class CSVUploadScreen extends StatelessWidget {
  const CSVUploadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CSV Upload Screen - Under Development'),
      ),
    );
  }
}

class TestTransactionIngestionScreen extends StatelessWidget {
  const TestTransactionIngestionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Test Transaction Ingestion Screen - Under Development'),
      ),
    );
  }
}

class ToolsScreen extends StatelessWidget {
  const ToolsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Tools Screen - Under Development'),
      ),
    );
  }
}

class AddGoalScreen extends StatelessWidget {
  const AddGoalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Add Goal Screen - Under Development'),
      ),
    );
  }
}

class GoalsScreen extends StatelessWidget {
  const GoalsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Goals Screen - Under Development'),
      ),
    );
  }
}

class CreateBudgetScreen extends StatelessWidget {
  const CreateBudgetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Create Budget Screen - Under Development'),
      ),
    );
  }
}

class SmsReviewScreen extends StatelessWidget {
  const SmsReviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('SMS Review Screen - Under Development'),
      ),
    );
  }
}

class LoanCalculatorScreen extends StatelessWidget {
  const LoanCalculatorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Loan Calculator Screen - Under Development'),
      ),
    );
  }
}

class ProgressiveGoalWizardScreen extends StatelessWidget {
  const ProgressiveGoalWizardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Progressive Goal Wizard Screen - Under Development'),
      ),
    );
  }
}

class SmartGoalCreationScreen extends StatelessWidget {
  const SmartGoalCreationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Smart Goal Creation Screen - Under Development'),
      ),
    );
  }
}

class AddTransactionScreen extends StatelessWidget {
  const AddTransactionScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Add Transaction Screen - Under Development'),
      ),
    );
  }
}

// Widget stubs
class QuickTransactionEntry extends StatelessWidget {
  const QuickTransactionEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Padding(
        padding: EdgeInsets.all(16.0),
        child: Text('Quick Transaction Entry - Under Development'),
      ),
    );
  }
}

// Method stubs for classes
class BudgetManagementScreen extends StatelessWidget {
  final dynamic budget;
  
  const BudgetManagementScreen({super.key, this.budget});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Budget Management Screen - Under Development'),
      ),
    );
  }
}

class GoalDetailsScreen extends StatelessWidget {
  final dynamic goal;
  
  const GoalDetailsScreen({super.key, this.goal});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Goal Details Screen - Under Development'),
      ),
    );
  }
}
